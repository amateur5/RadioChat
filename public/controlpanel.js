// Primer koda za umetanje slike iz URL-a
function addImageFromUrl(url) {
    const img = document.createElement('img');
    img.src = url;
    document.body.appendChild(img); // prilagodi gde želiš da slika bude
}

// Funkcija za umetanje FCP datoteke
function addFCPFile(file) {
    // Dodaj logiku za obradu FCP datoteke
}

// Funkcija za pomeranje chat i kontrolne table
function moveElement(elementId) {
    const element = document.getElementById(elementId);
    // Dodaj logiku za pomeranje
}

// Dodaj sve ostale funkcionalnosti
